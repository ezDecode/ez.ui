{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "vertical-dot-toc",
  "type": "registry:block",
  "title": "vertical-dot-toc",
  "description": "A component from ez-ui",
  "dependencies": ["motion", "next", "react"],
  "files": [
    {
      "path": "registry/new-york/vertical-dots/vertical-dot-toc.tsx",
      "content": "import { useState, useEffect, useCallback } from 'react';\r\nimport { motion, AnimatePresence, MotionConfig } from 'motion/react';\r\nimport { VerticalDotProgress, type DotItem } from './vertical-dot-progress';\r\n\r\ninterface VerticalDotTocProps {\r\n    items: DotItem[];\r\n    /** Duration in ms for each dot fill. Default = 3000 */\r\n    fillDuration?: number;\r\n    /** Whether to loop after reaching the last item. Default = true */\r\n    loop?: boolean;\r\n}\r\n\r\n// Blur + slide animation variants\r\nconst textVariants = {\r\n    initial: {\r\n        y: 12,\r\n        filter: 'blur(4px)',\r\n        opacity: 0,\r\n    },\r\n    animate: {\r\n        y: 0,\r\n        filter: 'blur(0px)',\r\n        opacity: 1,\r\n    },\r\n    exit: {\r\n        y: -12,\r\n        filter: 'blur(4px)',\r\n        opacity: 0,\r\n    },\r\n};\r\n\r\nexport function VerticalDotToc({ items, fillDuration = 3000, loop = true }: VerticalDotTocProps) {\r\n    const [activeIndex, setActiveIndex] = useState(0);\r\n\r\n    const advance = useCallback(() => {\r\n        setActiveIndex(prev => {\r\n            const next = prev + 1;\r\n            if (next >= items.length) {\r\n                return loop ? 0 : prev;\r\n            }\r\n            return next;\r\n        });\r\n    }, [items.length, loop]);\r\n\r\n    useEffect(() => {\r\n        const timer = setTimeout(advance, fillDuration);\r\n        return () => clearTimeout(timer);\r\n    }, [activeIndex, advance, fillDuration]);\r\n\r\n    const handleDotClick = (index: number) => {\r\n        setActiveIndex(index);\r\n    };\r\n\r\n    const activeItem = items[activeIndex];\r\n    const number = String(activeIndex + 1).padStart(2, '0');\r\n\r\n    return (\r\n        <MotionConfig transition={{ type: 'spring', duration: 0.4, bounce: 0 }}>\r\n            <div\r\n                className=\"flex items-start gap-4 rounded-lg p-4 shadow-2xl w-[20rem] transition-colors duration-500\"\r\n                style={{\r\n                    backgroundColor: 'var(--card-bg)',\r\n                    border: '1px solid var(--card-border)',\r\n                }}\r\n            >\r\n                {/* Vertical Dots */}\r\n                <VerticalDotProgress\r\n                    items={items}\r\n                    activeIndex={activeIndex}\r\n                    fillDuration={fillDuration}\r\n                    onDotClick={handleDotClick}\r\n                />\r\n\r\n                {/* Linked Text — swaps with blur + slide on each advance */}\r\n                <div className=\"flex-1 min-w-0 overflow-hidden\">\r\n                    <p\r\n                        className=\"text-[10px] font-mono font-normal tracking-normal uppercase mb-2\"\r\n                        style={{ color: 'var(--muted)' }}\r\n                    >\r\n                        On This Page\r\n                    </p>\r\n\r\n                    <AnimatePresence mode=\"popLayout\" initial={false}>\r\n                        <motion.div\r\n                            key={activeItem.id}\r\n                            variants={textVariants}\r\n                            initial=\"initial\"\r\n                            animate=\"animate\"\r\n                            exit=\"exit\"\r\n                            className=\"flex items-baseline gap-3\"\r\n                        >\r\n                            <span\r\n                                className=\"text-[10px] font-mono shrink-0\"\r\n                                style={{ color: 'var(--label-fg)' }}\r\n                            >\r\n                                {number}\r\n                            </span>\r\n                            <span\r\n                                className=\"text-xs font-mono uppercase tracking-normal leading-tight\"\r\n                                style={{ color: 'var(--dot-fill)' }}\r\n                            >\r\n                                {activeItem.label}\r\n                            </span>\r\n                        </motion.div>\r\n                    </AnimatePresence>\r\n\r\n                </div>\r\n            </div>\r\n        </MotionConfig>\r\n    );\r\n}\r\n\r\n",
      "type": "registry:component",
      "target": "components/ui/vertical-dots/vertical-dot-toc.tsx"
    }
  ]
}
