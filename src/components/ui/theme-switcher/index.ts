export { ThemeSwitcher } from './theme-switcher'
export { ThemeProvider, useTheme } from './theme-context'

export const metadata = {
  id: 'theme-switcher',
  name: 'Theme Switcher',
  description: 'Minimal theme switcher with sliding pill animation.',
} as const
